# 🚀 Инструкция по деплою на GitHub Pages

## Вариант 1: Автоматический деплой через GitHub Actions (Рекомендуется)

### Шаг 1: Добавьте секрет TMDB API ключа

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Name: `TMDB_API_KEY`
5. Value: Ваш API ключ от TMDB
6. Нажмите **Add secret**

### Шаг 2: Настройте GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: выберите **gh-pages** и **/ (root)**
4. Нажмите **Save**

### Шаг 3: Обновите package.json

Откройте `/frontend/package.json` и добавьте поле `homepage`:

```json
{
  "name": "frontend",
  "homepage": "https://ваш-username.github.io/название-репозитория",
  ...
}
```

### Шаг 4: Запушьте изменения

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push origin main
```

GitHub Actions автоматически соберет и задеплоит ваше приложение!

### Проверка статуса деплоя

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Ваш сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория`

---

## Вариант 2: Ручной деплой через gh-pages

### Шаг 1: Установите gh-pages

```bash
cd frontend
yarn add -D gh-pages
```

### Шаг 2: Добавьте скрипты в package.json

```json
{
  "scripts": {
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  }
}
```

### Шаг 3: Добавьте homepage

```json
{
  "homepage": "https://ваш-username.github.io/название-репозитория"
}
```

### Шаг 4: Убедитесь что .env настроен

В `/frontend/.env` должны быть:

```env
REACT_APP_TMDB_API_KEY=ваш_api_ключ
REACT_APP_TMDB_BASE_URL=https://api.themoviedb.org/3
REACT_APP_TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p
```

### Шаг 5: Выполните деплой

```bash
cd frontend
yarn deploy
```

### Шаг 6: Настройте GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: выберите **gh-pages** и **/ (root)**
4. Нажмите **Save**

Ваш сайт будет доступен через несколько минут!

---

## 🔧 Устранение проблем

### Проблема: 404 при переходе по страницам

**Решение**: Убедитесь, что используется `HashRouter` (уже настроено в проекте). URL будут выглядеть как `/#/browse` вместо `/browse`.

### Проблема: Не загружаются изображения

**Решение**: Проверьте, что TMDB API ключ корректно установлен в GitHub Secrets или в `.env` файле.

### Проблема: Белый экран после деплоя

**Решение**: Убедитесь, что поле `homepage` в `package.json` указывает на правильный URL вашего GitHub Pages.

### Проблема: GitHub Actions падает с ошибкой

**Решение**: 
1. Проверьте, что секрет `TMDB_API_KEY` добавлен
2. Убедитесь, что путь к `yarn.lock` корректен
3. Проверьте логи в разделе Actions

---

## 📱 Проверка деплоя

После успешного деплоя откройте:

```
https://ваш-username.github.io/название-репозитория
```

Приложение должно:
- ✅ Отображать главную страницу с фильмами
- ✅ Работать навигация
- ✅ Загружаться постеры
- ✅ Функционировать поиск
- ✅ Работать переключение тем

---

## 🎉 Готово!

Ваше приложение StreamX теперь доступно онлайн!

Поделитесь ссылкой:
```
https://ваш-username.github.io/название-репозитория
```
